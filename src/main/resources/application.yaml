#application.yaml
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
#    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update  # ? ideal para desenvolvimento
    show-sql: true
    defer-datasource-initialization: true  # ? permite que data.sql rode ap�s o schema ser criado
#    properties:
#      hibernate:
#        format_sql: true
  sql:
    init:
      mode: embedded
#    mode: always  # garante que data.sql seja executado
#  flyway:
#    enabled: false
#  autoconfigure:
#    exclude: org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration